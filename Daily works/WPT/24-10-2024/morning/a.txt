display property in css
display: none | block | inline | inline-block


none: element will be removed
block: element will a started from from new line
inline % inline block : elements will be displayed in same line

display: inline(then width & height property will have no effect)
if we want to persist  the custom width and height so we have to use display: inline-block

box-shadow in nav bar:
(, bottom thick, blur effect, color effect)

nav bar  gap can be eliminated:
margin : 0;

box-medal
height : 400px
border : 2px
padding-top: 20px bottom: 20px;
  400 + 2 + 2 + 20 + 20 


  box model is a pictorial representation of any element, which depicts the amount of margins, paddings, borders and width & height occupied by any element

  the total width : original width + padding + border + padding